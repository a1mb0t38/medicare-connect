
import React from 'react';

const AdminPage = async() => {

    // const res = await fetch(`${process.env.BASE_URL}/admin/overview`, {cache: "no-store"})
    // const data = await res.json()

    return (
        <div>
          
        </div>
    );
};

export default AdminPage;